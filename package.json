{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --detectOpenHandles --runInBand --silent",
    "test:verbose": "jest --detectOpenHandles --runInBand",
    "test:coverage": "jest --detectOpenHandles --runInBand --coverage",
    "test:smoke": "jest --detectOpenHandles --runInBand tests/smoke.test.ts",
    "test:dev": "cls && jest --detectOpenHandles --runInBand --silent",
    "dev": "bun run .dev/dev.ts",
    "build": "npm run build:esm && npm run build:cjs && postbuild",
    "build:esm": "npx tsc -p tsconfig.esm.json",
    "build:cjs": "npx tsc -p tsconfig.cjs.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9"
  }
}
